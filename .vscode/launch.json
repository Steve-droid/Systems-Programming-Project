{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Assembler",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/assembler",
            "args": ["ex1", "ex2"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
